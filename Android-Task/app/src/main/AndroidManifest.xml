<?xml version="1.0" encoding="utf-8"?>
<manifest package="p.androidtask"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.android.TasksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.android.ui.tasks.TasksActivity"
            android:label="Task"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.android.ui.detail.DetailActivity"
            android:label="@string/task_details"/>
        <activity
            android:name="com.android.ui.add.AddTaskActivity"
            android:label="@string/add_new_task"/>

    </application>

</manifest>
